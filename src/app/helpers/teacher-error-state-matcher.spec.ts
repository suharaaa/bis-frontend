import { TeacherErrorStateMatcher } from './teacher-error-state-matcher';

describe('TeacherErrorStateMatcher', () => {
  it('should create an instance', () => {
    expect(new TeacherErrorStateMatcher()).toBeTruthy();
  });
});
