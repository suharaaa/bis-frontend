import { StudentErrorStateMatcher } from './student-error-state-matcher';

describe('StudentErrorStateMatcher', () => {
  it('should create an instance', () => {
    expect(new StudentErrorStateMatcher()).toBeTruthy();
  });
});
